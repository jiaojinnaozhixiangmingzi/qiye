.panel.panel-default
  .panel-heading
    | 品类价格
  .panel-body
    table.table.table-striped
      thead
        tr
          th Name
          th

      tbody
        - @categories.each do |category|
          tr
            td = category.name
            td = link_to '价格信息', prices_category_path(category)

    = will_paginate @categories, renderer: BootstrapPagination::Rails